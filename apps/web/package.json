{
	"name": "prq-web",
	"private": true,
	"scripts": {
		"dev": "bun --hot src/server.ts",
		"build": "bun src/build-changelog.ts && rm -rf dist && cp -r public dist && bun build src/index.html src/changelog.html --outdir dist",
		"typecheck": "tsc --noEmit"
	}
}
